# Moltagent Development Rules

The development rules live in one place: the auto-loaded Claude Code skill at `.claude/skills/moltagent-dev-rules/SKILL.md`. That skill is the single source of truth.

Edit the rules there, not here. This file carries no rule content, so it cannot drift out of sync. It exists only so that links and habits pointing at `moltagent-dev-rules.md` still find their way to the rules.
